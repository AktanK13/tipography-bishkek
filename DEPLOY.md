# Инструкция по деплою на хостинг

## Шаг 1: Подготовка проекта локально

### 1.1. Убедитесь, что все зависимости установлены
```bash
npm install
```

### 1.2. Соберите проект для продакшена
```bash
npm run build
```

После выполнения этой команды будет создана папка `dist/` со всеми файлами для деплоя.

### 1.3. Проверьте сборку локально (опционально)
```bash
npm run preview
```

Откройте браузер и проверьте, что всё работает корректно.

---

## Шаг 2: Подготовка файлов для загрузки

### 2.1. Скопируйте файл .htaccess в папку dist
```bash
cp .htaccess dist/
```

Этот файл нужен для правильной работы React Router на Apache серверах.

### 2.2. Проверьте содержимое папки dist
Убедитесь, что в папке `dist/` есть:
- `index.html`
- `assets/` (папка с JS, CSS и изображениями)
- Все файлы из папки `public/` (background-header.jpg, homeBackground.png, robots.txt, sitemap.xml и т.д.)
- `.htaccess` (который вы только что скопировали)

---

## Шаг 3: Загрузка на хостинг

### Вариант A: Через FTP/SFTP

1. **Подключитесь к хостингу через FTP клиент** (FileZilla, WinSCP, Cyberduck и т.д.)
   - Хост: ваш FTP адрес
   - Пользователь: ваш FTP логин
   - Пароль: ваш FTP пароль
   - Порт: обычно 21 (FTP) или 22 (SFTP)

2. **Найдите папку для сайта**
   - Обычно это `public_html/` или `www/` или `htdocs/`
   - Уточните у вашего хостинг-провайдера

3. **Загрузите ВСЁ содержимое папки `dist/`**
   - Выберите все файлы из папки `dist/`
   - Загрузите их в корневую папку сайта на хостинге
   - ⚠️ **ВАЖНО**: Загружайте именно СОДЕРЖИМОЕ папки `dist/`, а не саму папку `dist`

### Вариант B: Через панель управления хостингом (cPanel, ISPmanager и т.д.)

1. Войдите в панель управления хостингом
2. Откройте файловый менеджер
3. Перейдите в папку `public_html/` (или аналогичную)
4. Загрузите все файлы из папки `dist/` через веб-интерфейс

### Вариант C: Через SSH (если есть доступ)

```bash
# Создайте архив
cd dist
tar -czf ../deploy.tar.gz .

# Загрузите на сервер через SCP
scp deploy.tar.gz user@your-server.com:/path/to/public_html/

# На сервере распакуйте
ssh user@your-server.com
cd /path/to/public_html/
tar -xzf deploy.tar.gz
rm deploy.tar.gz
```

---

## Шаг 4: Настройка на хостинге

### Для Apache серверов (большинство хостингов)

1. **Убедитесь, что файл `.htaccess` загружен** в корневую папку сайта
2. **Проверьте, что mod_rewrite включен** (обычно включен по умолчанию)
3. Если сайт не работает, обратитесь в поддержку хостинга с просьбой включить mod_rewrite

### Для Nginx серверов

Если ваш хостинг использует Nginx, вам нужно:

1. **Создать или отредактировать конфигурацию Nginx**
   - Файл конфигурации обычно находится в `/etc/nginx/sites-available/your-site`
   - Используйте файл `nginx.conf` из корня проекта как пример

2. **Основное правило для Nginx:**
   ```nginx
   location / {
       try_files $uri $uri/ /index.html;
   }
   ```

3. **Перезагрузите Nginx:**
   ```bash
   sudo nginx -t  # Проверка конфигурации
   sudo systemctl reload nginx
   ```

### Настройка домена

1. **Убедитесь, что домен указывает на правильную папку**
   - В панели управления хостингом проверьте настройки домена
   - Домен должен указывать на папку `public_html/` (или аналогичную)

2. **Настройте SSL сертификат (HTTPS)**
   - Большинство хостингов предоставляют бесплатный SSL через Let's Encrypt
   - Включите SSL в панели управления хостингом

---

## Шаг 5: Проверка после деплоя

1. **Откройте сайт в браузере**
   - Проверьте главную страницу
   - Проверьте все маршруты (главная, портфолио, о компании, контакты)
   - Проверьте страницы товаров

2. **Проверьте консоль браузера (F12)**
   - Убедитесь, что нет ошибок загрузки файлов
   - Проверьте, что все изображения загружаются

3. **Проверьте мобильную версию**
   - Откройте сайт на мобильном устройстве
   - Проверьте адаптивность

---

## Частые проблемы и решения

### Проблема: Страница показывает "404 Not Found" при переходе по ссылкам

**Решение:**
- Убедитесь, что файл `.htaccess` загружен в корневую папку сайта
- Проверьте, что mod_rewrite включен на сервере
- Для Nginx используйте правильную конфигурацию

### Проблема: Изображения не загружаются

**Решение:**
- Проверьте, что папка `assets/` загружена полностью
- Убедитесь, что пути к изображениям правильные (начинаются с `/assets/`)
- Проверьте права доступа к файлам (должны быть 644 для файлов, 755 для папок)

### Проблема: Стили не применяются

**Решение:**
- Очистите кэш браузера (Ctrl+Shift+R или Cmd+Shift+R)
- Проверьте, что файлы CSS загружены в папку `assets/`
- Проверьте консоль браузера на наличие ошибок 404

### Проблема: Сайт работает, но медленно

**Решение:**
- Включите Gzip сжатие (уже настроено в .htaccess)
- Убедитесь, что кэширование работает
- Проверьте размер изображений (они должны быть оптимизированы)

---

## Обновление сайта

Когда нужно обновить сайт:

1. Внесите изменения в код
2. Выполните `npm run build`
3. Загрузите обновлённые файлы из папки `dist/` на хостинг
4. Очистите кэш браузера и проверьте изменения

---

## Дополнительные настройки

### Настройка robots.txt и sitemap.xml

Файлы `robots.txt` и `sitemap.xml` уже должны быть в папке `dist/` после сборки.

Убедитесь, что они доступны по адресам:
- `https://ваш-домен.kg/robots.txt`
- `https://ваш-домен.kg/sitemap.xml`

---

## Контакты поддержки

Если возникли проблемы:
1. Проверьте логи ошибок на хостинге
2. Обратитесь в поддержку хостинг-провайдера
3. Убедитесь, что все файлы загружены корректно

---

**Удачи с деплоем! 🚀**

