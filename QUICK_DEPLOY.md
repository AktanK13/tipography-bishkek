# 🚀 Быстрая инструкция по деплою

## Шаг 1: Соберите проект для деплоя

Выполните в терминале:
```bash
npm run build:deploy
```

Эта команда:
- Соберёт проект для продакшена
- Скопирует файл `.htaccess` в папку `dist/`

## Шаг 2: Загрузите файлы на хостинг

### Что загружать:
**ВСЁ содержимое папки `dist/`** (не саму папку, а её содержимое!)

### Куда загружать:
- Обычно это папка `public_html/` или `www/` на вашем хостинге
- Уточните у вашего хостинг-провайдера

### Как загружать:
1. **Через FTP/SFTP** (FileZilla, WinSCP и т.д.)
   - Подключитесь к хостингу
   - Откройте папку `public_html/`
   - Загрузите все файлы из папки `dist/`

2. **Через панель управления** (cPanel, ISPmanager)
   - Откройте файловый менеджер
   - Перейдите в `public_html/`
   - Загрузите файлы через веб-интерфейс

## Шаг 3: Настройка на хостинге

### Для Apache (большинство хостингов):
✅ Файл `.htaccess` уже должен быть загружен вместе с остальными файлами
✅ Обычно всё работает автоматически

### Для Nginx:
Если ваш хостинг использует Nginx, обратитесь в поддержку с просьбой настроить:
```nginx
location / {
    try_files $uri $uri/ /index.html;
}
```

## Шаг 4: Проверка

1. Откройте ваш сайт в браузере
2. Проверьте все страницы (главная, портфолио, контакты)
3. Проверьте мобильную версию

## ❓ Если что-то не работает:

1. **Страница 404 при переходе по ссылкам:**
   - Убедитесь, что файл `.htaccess` загружен
   - Обратитесь в поддержку хостинга

2. **Изображения не загружаются:**
   - Проверьте, что папка `assets/` загружена полностью
   - Проверьте права доступа к файлам (644 для файлов, 755 для папок)

3. **Стили не применяются:**
   - Очистите кэш браузера (Ctrl+Shift+R)
   - Проверьте консоль браузера (F12) на ошибки

---

📖 **Подробная инструкция:** см. файл `DEPLOY.md`

